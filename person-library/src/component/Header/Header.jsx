// import s from './Header.module.css'
const Header = (props) =>{
    return (
        <header className={props.myStyle}>
            {/* <div className={props.texStyle}>
           <p> {props.text} </p>
           </div> */}
        </header>
        );

};

export default Header;